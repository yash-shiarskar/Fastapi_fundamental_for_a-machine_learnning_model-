# Fastapi_fundamental_for_a-machine_learnning_model-
A RESTful Patient Management API built with FastAPI featuring CRUD operations, Pydantic validation, computed BMI logic, sorting, and JSON-based persistence.
